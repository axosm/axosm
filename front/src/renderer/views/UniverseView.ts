import { BaseView } from "./BaseView";

export class UniverseView extends BaseView {
    constructor() {
        super();
    }
    
    public update(delta: number): void {
        throw new Error("Method not implemented.");
    }
}
